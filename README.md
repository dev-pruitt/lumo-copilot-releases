# Lumo Copilot — Releases

Auto-update DMGs for Lumo Copilot, a local-first macOS AI copilot. The app checks this repo's latest Release on launch and offers a one-click update.

Each release is tagged `vX.Y.Z` with the arm64 `.dmg` attached. Unsigned — first launch: right-click -> Open.
